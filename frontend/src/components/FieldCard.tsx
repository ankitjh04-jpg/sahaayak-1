import { Link } from 'react-router-dom';
import { ArrowUpRight, MapPin, Wheat, Flower2, Sprout } from 'lucide-react';
import { FarmField } from '../types';
import { useApp } from '../app/context';
export const FieldCard = ({field}: {field: FarmField}) => {const {t} = useApp(); const days = Math.max(0,Math.floor((Date.now()-new Date(field.sowing_date).getTime())/86400000)); const Icon = field.crop === 'Wheat' ? Wheat : field.crop === 'Mustard' ? Flower2 : Sprout; return <Link className={`field-card ${field.crop.toLowerCase()}`} data-testid={`field-card-${field.id}`} to={`/fields/${field.id}`}><div className="field-card-top"><span className="field-illustration"><Icon size={38} strokeWidth={1.2}/></span><span className="crop-tag" data-testid={`field-crop-${field.id}`}>{field.crop}</span><ArrowUpRight size={18}/></div><h3 data-testid={`field-name-${field.id}`}>{field.name}</h3><p className="field-location"><MapPin size={13}/>{field.location}</p><div className="field-card-footer"><span data-testid={`field-acres-${field.id}`}><strong>{field.acreage}</strong> {t('acres','एकड़','ਏਕੜ')}</span><span className="field-day" data-testid={`field-days-${field.id}`}><i/>{t('Day','दिन','ਦਿਨ')} {days} {t('since sowing','बुवाई से','ਬਿਜਾਈ ਤੋਂ')}</span></div></Link>;};